# Cấu hình Docker Compose cho hệ thống Quản lý Sinh viên
services:
  qlsv-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qlsv-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local            
    restart: unless-stopped   
